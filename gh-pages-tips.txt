0- npm i gh-pages

1- AppRouter.js
function AppRouter() {
  return (
    <HashRouter>
        <Header />
        <div className="wrapper">
          <Routes>
            <Route path="/" exact element={<Home />} />
            <Route path="/react-project" exact element={<Home />} />


2- package.json
  "homepage": "https://tonykielingbcit.github.io/react-project/",
...
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",


3- Header
	href="/react-project" 
      className = {`single-item ${(currentLocation === "/react-project" ? "is-active" : "")}`}


4- index.js
  <React.StrictMode>
      <AppRouter />
  </React.StrictMode>

5- rpm run deploy - maybe npm run build before